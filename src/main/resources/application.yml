spring:
  webflux:
    base-path: /api
  data:
    r2dbc:
      repositories:
        enabled: true
  r2dbc:
    url: r2dbc:postgresql://database-instance.cq35i9ttyarx.eu-north-1.rds.amazonaws.com/ccs-tech-db
    username: ccsTechUser
    password: ccsTechPass
    pool:
      name: TEST-POOL
      initial-size: 1
      max-size: 10
      max-idle-time: 30m
    properties:
      schema: public
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  liquibase:
    enabled: true
    url: jdbc:postgresql://database-instance.cq35i9ttyarx.eu-north-1.rds.amazonaws.com/ccs-tech-db?useSSL=false
    user: ccsTechUser
    password: ccsTechPass
    change-log: classpath:/db/changelog-master.xml
    default-schema: public

app:
  jwt:
    token-expiration-seconds: 1800
    secret-key: JWTSuperSecretKey
  credit-card-amount-percentage: 1.0
  debit-card-amount-percentage: 0.0